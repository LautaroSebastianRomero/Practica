Apunte GitHub:

-Pasos:
1) Logearse: git config --global user.name "Usuario"
             git config --global user.email "Email"

2)Iniciar Repositorio: git init (dentro de la carpeta)

3)Revisar destino: git remote -v
si hay origen: git remote remove origin
Añadir origen: git remote add origin "link"

revisar estado de git: git status

4)Se empieza con el guardado
git add "nombre": añade los cambios
git commit -m "nombre": agrega un nombre
git branch -M main(no es un paso del guardado): arma una rama
git push -u origin main: guarda los cambios-termina de subir


git clone (link del repo)
